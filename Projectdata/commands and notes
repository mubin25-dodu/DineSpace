setting .env 
npm i @nestjs/config

setting jwt 

install this packages
    npm i @nestjs/jwt @nestjs/passport passport passport-jwt passport-local

import in module 

import[jwtModule.register({
    secret: the secret string ,
    signinOption:{expireseIn: '1h'}
})]

dependency injection 
in constructor inject "JwtService

in the service  this.JwtService.sign(theuserobject) creating the tocken 


import the passportmodule it does the heavy lifting 
 [passportmodule]


//setting PostgreSQL
npm install @nestjs/typeorm typeorm pg 
npm install class-validator class-transformer 
npm install @nestjs/mapped-types 

pass =  securePassword123!